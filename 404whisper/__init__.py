"""
404Whisper — decentralised, end-to-end encrypted messaging built on the Session protocol.

Package entry point.  Import sub-packages directly; this file keeps the top-level
namespace intentionally minimal so nothing is accidentally exposed at the root.
"""
from __future__ import annotations

# Human-readable version string — bump this on every release.
__version__ = "0.1.0"
